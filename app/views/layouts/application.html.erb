<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Chat App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= javascript_include_tag "https://code.jquery.com/jquery-3.6.4.min.js", crossorigin: "anonymous" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light px-3">
      <a class="navbar-brand" href="/">ChatApp</a>
      <%# <ul class="navbar-nav me-auto"> %>
        <%# ChatRoom.all.each do |room| %>
          <%# <li class="nav-item"> %>
            <%#= link_to room.name, chat_room_path(room), class: "nav-link" %>
          <%# </li> %>
        <%# end %>
      </ul>
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <% if current_user %>
            <%= current_user.email %> |
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link d-inline" %>
          <% end %>
        </li>
      </ul>
    </nav>
    <% if current_user %>
      <div class="container mt-3">
        <h5>Online Users</h5>
        <ul id="user-list"></ul>
      </div>
    <% end %>
    <%= yield %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
  </body>
</html>
